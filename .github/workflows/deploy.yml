name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
        
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: '**/package-lock.json'
        
    - name: Install dependencies
      run: |
        npm ci
        npm run install:all
        
    - name: Build applications
      run: npm run build
      
    - name: Create deployment structure
      run: |
        mkdir -p deploy
        # Main app goes to root of deployment
        cp -r main-app/dist/* deploy/
        # Remote apps go to subdirectories
        mkdir -p deploy/remoteA
        mkdir -p deploy/remoteB
        cp -r remoteA/dist/* deploy/remoteA/
        cp -r remoteB/dist/* deploy/remoteB/
        
    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v4
      if: github.ref == 'refs/heads/main'
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./deploy
